

import PropTypes from 'prop-types';

const WantToCook = ({cookingRecipe}) => {
    console.log(cookingRecipe)
    return (
        <div>
            pdfsgfghdth
        </div>
    )
};

WantToCook.propTypes = {
    cookingRecipe: PropTypes.object
};

export default WantToCook;